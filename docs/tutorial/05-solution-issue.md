# 【解答例①】実装の解説

> **この章の目的（存在理由）**
> 自力実装の答え合わせ。なぜその設計で要件を満たせるのかを言語化し、実際に発行して確かめる。

<!-- SKELETON (Step2): 以下は記載アウトライン。Step3 で本文化する。 -->

## 解答例のコード

- 解答例 `UserIdGenerator` の `issue()` を引用して解説

## なぜ要件を満たすのか

- 時刻成分を先頭に置く → 発行順にソート可能
- 通番＋乱数で同一ミリ秒を一意化 → 連番回避＆衝突回避
- base62 で10文字にエンコード

## 📊 図: ID生成の流れ

- 時刻・通番 → 整数 → base62 の変換フロー（Step3 で作図）

## 動かして確認

- Swagger（自分で実装した app(8000) か、解答例 solution(8001)）で発行
- web(5173) で一覧・妥当性バッジを確認
- （スケールの話はまだ出さない）
