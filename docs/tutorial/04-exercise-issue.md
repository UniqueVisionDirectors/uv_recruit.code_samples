# 【問題①】ID発行エンドポイントを実装する

> **この章の目的（存在理由）**
> 最初の穴埋め演習。前章の要件を満たすユニークID発行を、読者が自力で実装する。

<!-- SKELETON (Step2): 以下は記載アウトライン。Step3 で本文化する。 -->

## 問題

- 「ユーザーがアクセスしたら**ユニークなUserID**を発行するエンドポイントを実装してください」
- 実装する場所: `app/idgen/problem.py` の `ProblemIssuer.issue`
- 満たすべき要件（前章のID要件を再掲）

## まず演習テストを走らせる（今は red）

- 演習テスト（別枠 `exercise` マーカー）を実行するコマンド
- 期待: 未実装なので **失敗(red)**。「まだ解いていない」ことがテストで分かる
- ⚠️ 演習テストの実体（red→green）は **Task#2** で用意してから本文を確定する

## 実装のヒント

- 連番回避・発行順ソート可をどう満たすかの方向性（答えは出しすぎない）

## 実装できたら再度テスト（green）

- 期待: **成功(green)**。要件を満たせたことをテストが保証
