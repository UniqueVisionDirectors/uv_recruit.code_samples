# スケールとロードバランサー

> **この章の目的（存在理由）**
> 「1台では捌けない」という新しい問題を導入し、水平スケールとロードバランサー(LB)を説明する。ここで初めて「今までの実装は“1台で動く”ことを暗黙に前提していた」と振り返り、次章の問題②へ橋渡しする。

<!-- SKELETON (Step2): 以下は記載アウトライン。Step3 で本文化する。 -->

## アクセスが増えると1台では捌けない

- 負荷集中の問題。垂直スケール（増強）と水平スケール（台数を増やす）の対比

## 水平スケールとロードバランサー(LB)

- 同じアプリを複数台並べ、LB がリクエストを振り分ける
- 📊 図: stage1 構成（ブラウザ → web → LB → app×3 → DB）

## 振り返り：今までの実装の隠れた前提

- 「実は今までの実装は“1台で動く”ことを暗黙に前提していた」と明かす
- 同じ実装を複数台に並べたら何が起きる？ → 次章で体験

## デモスタックを起動する

- `ID_STRATEGY=stage1 docker compose -f compose.yaml -f compose.demo.yaml up ...`
- stage1 = 素朴な実装（worker を区別しない）を3台に並べた状態、という位置づけを説明
- ⚠️ 「読者のコード(problem)」と「demoのstage1」の関係づけは **Task#2** で整理する
