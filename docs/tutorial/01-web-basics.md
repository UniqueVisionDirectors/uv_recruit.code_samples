# Webアプリの基本構成

> **この章の目的（存在理由）**
> まず「Webアプリとは何でできているか」を抽象的に掴む。以降の章で扱う具体例（Vue / FastAPI / PostgreSQL）を載せるための“枠”を、ここで先に作っておく。

<!-- SKELETON (Step2): 以下は記載アウトライン。Step3 で本文化する。 -->

## Webアプリは3つの層でできている

- フロントエンド(FE) / バックエンド(BE) / データベース(DB) の役割を、専門用語を噛み砕いて説明する
- 1リクエストがどう流れるか（ブラウザ → FE → BE → DB → BE → FE）を順を追って

## 📊 図: 3層構成

- ブラウザ → FE → BE → DB の流れを Mermaid で（Step3 で作図）

## この教材で学ぶこと（予告）

- 「ユーザーIDを発行する」という身近な題材で、層をまたいだ設計を手を動かして学ぶ、と一言だけ
- （この時点ではスケール等の先の概念には触れない）
