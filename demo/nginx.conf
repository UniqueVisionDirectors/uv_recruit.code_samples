events {}
http {
  upstream app_backend {
    server app1:8000;
    server app2:8000;
    server app3:8000;
  }
  server {
    listen 8080;
    location / {
      proxy_pass http://app_backend;
    }
  }
}
